<h1>Tag#index</h1>
<p>Find me in app/views/tag/index.html.erb</p>

<div id="app" class="container">
  <div>
    tag:tag
    tags:{{tags
    message:message
    handlers:handlers
  </div>
  <div>
    <button class="btn btn-warning">warn</button>
    <button class="btn btn-info">info</button>
  </div>
  <div v-if="true">max</div>
  <vue-tags-input
    v-model="tag"
    :tags="tags"
    :allow-edit-tags="true"
    :avoid-adding-duplicates="false"
    :max-tags="40"
    :maxlength="1116"
    placeholder="input tag"
    :validation="validation"
    @tags-changed="newTags => tags = newTags"
    @input="inputcheck($event)"
    @max-tags-reached="maxtags"
    />
  <div>ssss</div>
  <div v-if="handlers.length">
    <div>wwwwwwww</div>
  </div>
</div>

<%= javascript_pack_tag 'tag_vue' %>